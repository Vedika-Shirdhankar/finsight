import { t as supabase } from "./client-PdxCd9pa.js";
import { t as queryKeys } from "./keys-C2024mUc.js";
import { n as useProfile, t as getNotificationPreferences } from "./use-profile-BkKF9vVe.js";
import { t as useCategories } from "./use-categories-BSfFvBVL.js";
import { n as useBudget } from "./use-budgets-B4foJmW9.js";
import { r as useSavingsGoals } from "./use-savings-goals-pV_2YW7V.js";
import { r as useTransactions } from "./use-transactions-Bc0pgBXv.js";
import { t as detectAnomalies } from "./analytics-BcG96liB.js";
import { useEffect, useMemo, useRef } from "react";
import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
//#region src/hooks/queries/use-notifications.ts
/** Fetches the signed-in user's notifications, most recent first. */
function useNotifications(userId) {
	return useQuery({
		queryKey: queryKeys.notifications(userId ?? ""),
		queryFn: async () => {
			const { data, error } = await supabase.from("notifications").select("*").order("created_at", { ascending: false }).limit(50);
			if (error) throw error;
			return data;
		},
		enabled: !!userId
	});
}
function useMarkNotificationRead(userId) {
	const queryClient = useQueryClient();
	return useMutation({
		mutationFn: async (id) => {
			const { error } = await supabase.from("notifications").update({ is_read: true }).eq("id", id);
			if (error) throw error;
		},
		onSuccess: () => {
			queryClient.invalidateQueries({ queryKey: queryKeys.notifications(userId ?? "") });
		}
	});
}
function useMarkAllNotificationsRead(userId) {
	const queryClient = useQueryClient();
	return useMutation({
		mutationFn: async () => {
			if (!userId) return;
			const { error } = await supabase.from("notifications").update({ is_read: true }).eq("user_id", userId).eq("is_read", false);
			if (error) throw error;
		},
		onSuccess: () => {
			queryClient.invalidateQueries({ queryKey: queryKeys.notifications(userId ?? "") });
		}
	});
}
function monthEnd(monthStart) {
	const d = /* @__PURE__ */ new Date(monthStart + "T00:00:00");
	d.setMonth(d.getMonth() + 1);
	d.setDate(0);
	return d.toISOString().slice(0, 10) + "T23:59:59";
}
var GOAL_MILESTONES = [
	50,
	75,
	100
];
var BUDGET_ALERT_PCT = 80;
/**
* Watches the current month's budget categories and savings goals, and
* writes a notification row the first time a threshold is crossed
* (80%/100% of a budget category, or a 50/75/100% savings-goal milestone).
* Dedupes against existing notifications for the same period so it's safe
* to mount this on every dashboard load without spamming the feed.
*/
function useNotificationSync(userId) {
	const monthStart = useMemo(() => (/* @__PURE__ */ new Date()).toISOString().slice(0, 8) + "01", []);
	const ninetyDaysAgo = useMemo(() => {
		const d = /* @__PURE__ */ new Date();
		d.setDate(d.getDate() - 90);
		return d.toISOString().slice(0, 10);
	}, []);
	const { data: budget } = useBudget(userId, monthStart);
	const { data: categories } = useCategories(userId);
	const { data: goals } = useSavingsGoals(userId);
	const { data: monthExpenses } = useTransactions(userId, {
		from: monthStart,
		to: monthEnd(monthStart),
		type: "expense"
	});
	const { data: recentExpenses } = useTransactions(userId, {
		from: ninetyDaysAgo,
		type: "expense"
	});
	const { data: existing } = useNotifications(userId);
	const { data: profile } = useProfile(userId);
	const prefs = getNotificationPreferences(profile);
	const queryClient = useQueryClient();
	const ranFor = useRef(null);
	useEffect(() => {
		if (!userId || !budget || !categories || !goals || !monthExpenses || !recentExpenses || !existing || !profile) return;
		const snapshotKey = `${budget.id}:${monthExpenses.length}:${recentExpenses.length}:${goals.map((g) => g.current_amount).join(",")}:${prefs.budget_alerts}:${prefs.spending_insights}`;
		if (ranFor.current === snapshotKey) return;
		ranFor.current = snapshotKey;
		const spendByCategory = /* @__PURE__ */ new Map();
		for (const t of monthExpenses) {
			if (!t.category_id) continue;
			spendByCategory.set(t.category_id, (spendByCategory.get(t.category_id) ?? 0) + Number(t.amount));
		}
		const categoryName = (id) => categories.find((c) => c.id === id)?.name ?? "a category";
		const toInsert = [];
		const existsAlready = (title, message) => existing.some((n) => n.title === title && n.message === message);
		for (const bc of budget.budget_categories) {
			if (!prefs.budget_alerts) break;
			const spent = spendByCategory.get(bc.category_id) ?? 0;
			const limit = Number(bc.limit_amount);
			if (limit <= 0) continue;
			const pct = spent / limit * 100;
			const name = categoryName(bc.category_id);
			if (pct >= 100) {
				const title = "Budget exceeded";
				const message = `${name} spending has gone over its ₹${limit.toLocaleString("en-IN")} monthly budget.`;
				if (!existsAlready(title, message)) toInsert.push({
					user_id: userId,
					title,
					message,
					kind: "warning"
				});
			} else if (pct >= BUDGET_ALERT_PCT) {
				const title = "Budget limit alert";
				const message = `${name} spending reached ${Math.round(pct)}% of its ₹${limit.toLocaleString("en-IN")} monthly budget.`;
				if (!existsAlready(title, message)) toInsert.push({
					user_id: userId,
					title,
					message,
					kind: "warning"
				});
			}
		}
		for (const goal of goals) {
			if (!prefs.spending_insights) break;
			const target = Number(goal.target_amount);
			if (target <= 0) continue;
			const pct = Number(goal.current_amount) / target * 100;
			for (const milestone of GOAL_MILESTONES) {
				if (pct < milestone) continue;
				const title = milestone === 100 ? "Savings goal reached" : "Savings milestone";
				const message = milestone === 100 ? `Congratulations! You reached your "${goal.name}" goal.` : `You're ${milestone}% of the way to your "${goal.name}" goal.`;
				if (!existsAlready(title, message)) toInsert.push({
					user_id: userId,
					title,
					message,
					kind: "positive"
				});
			}
		}
		if (prefs.spending_insights) {
			const categoryNameById = new Map(categories.map((c) => [c.id, c.name]));
			const anomalies = detectAnomalies(recentExpenses, categoryNameById);
			const sevenDaysAgo = /* @__PURE__ */ new Date();
			sevenDaysAgo.setDate(sevenDaysAgo.getDate() - 7);
			const recentAnomalies = anomalies.filter((a) => new Date(a.transaction.transaction_date) >= sevenDaysAgo).slice(0, 5);
			for (const a of recentAnomalies) {
				const title = "Unusual transaction";
				const message = `${a.transaction.merchant ?? "A transaction"} for ₹${Number(a.transaction.amount).toLocaleString("en-IN")} in ${a.categoryLabel} is much higher than your usual spending there.`;
				if (!existsAlready(title, message)) toInsert.push({
					user_id: userId,
					title,
					message,
					kind: "warning"
				});
			}
		}
		if (toInsert.length > 0) supabase.from("notifications").insert(toInsert).then(({ error }) => {
			if (!error) queryClient.invalidateQueries({ queryKey: queryKeys.notifications(userId) });
		});
	}, [
		userId,
		budget,
		categories,
		goals,
		monthExpenses,
		recentExpenses,
		existing,
		profile,
		prefs.budget_alerts,
		prefs.spending_insights,
		queryClient
	]);
}
//#endregion
//#region src/hooks/queries/use-recurring-transactions.ts
function useRecurringTransactions(userId) {
	return useQuery({
		queryKey: queryKeys.recurringTransactions(userId ?? ""),
		queryFn: async () => {
			const { data, error } = await supabase.from("recurring_transactions").select("*").order("next_due_date", { ascending: true });
			if (error) throw error;
			return data;
		},
		enabled: !!userId
	});
}
function useCreateRecurringTransaction(userId) {
	const queryClient = useQueryClient();
	return useMutation({
		mutationFn: async (input) => {
			if (!userId) throw new Error("Not signed in");
			const { data, error } = await supabase.from("recurring_transactions").insert({
				...input,
				user_id: userId
			}).select().single();
			if (error) throw error;
			return data;
		},
		onSuccess: () => {
			queryClient.invalidateQueries({ queryKey: queryKeys.recurringTransactions(userId ?? "") });
		}
	});
}
function useUpdateRecurringTransaction(userId) {
	const queryClient = useQueryClient();
	return useMutation({
		mutationFn: async ({ id, ...input }) => {
			const { data, error } = await supabase.from("recurring_transactions").update(input).eq("id", id).select().single();
			if (error) throw error;
			return data;
		},
		onSuccess: () => {
			queryClient.invalidateQueries({ queryKey: queryKeys.recurringTransactions(userId ?? "") });
		}
	});
}
function useDeleteRecurringTransaction(userId) {
	const queryClient = useQueryClient();
	return useMutation({
		mutationFn: async (id) => {
			const { error } = await supabase.from("recurring_transactions").delete().eq("id", id);
			if (error) throw error;
		},
		onSuccess: () => {
			queryClient.invalidateQueries({ queryKey: queryKeys.recurringTransactions(userId ?? "") });
		}
	});
}
function advanceDueDate(date, frequency) {
	const d = /* @__PURE__ */ new Date(date + "T00:00:00");
	switch (frequency) {
		case "weekly":
			d.setDate(d.getDate() + 7);
			break;
		case "biweekly":
			d.setDate(d.getDate() + 14);
			break;
		case "monthly":
			d.setMonth(d.getMonth() + 1);
			break;
		case "yearly": d.setFullYear(d.getFullYear() + 1);
	}
	return d.toISOString().slice(0, 10);
}
function daysUntil(date) {
	const today = (/* @__PURE__ */ new Date()).toISOString().slice(0, 10);
	const diffMs = (/* @__PURE__ */ new Date(date + "T00:00:00")).getTime() - (/* @__PURE__ */ new Date(today + "T00:00:00")).getTime();
	return Math.round(diffMs / 864e5);
}
/**
* Runs on dashboard load:
*  - any active recurring item whose next_due_date has arrived gets posted
*    as a real transaction and rolled forward to its next occurrence.
*  - any active recurring item due within its `remind_days_before` window
*    gets a one-time "upcoming bill" notification (deduped by title+message
*    against existing rows, same pattern as budget/goal alerts).
*/
function useTodayString() {
	return (/* @__PURE__ */ new Date()).toISOString().slice(0, 10);
}
function useRecurringTransactionSync(userId) {
	const { data: recurring } = useRecurringTransactions(userId);
	const { data: existingNotifications } = useNotifications(userId);
	const queryClient = useQueryClient();
	const ranFor = useRef(null);
	const today = useTodayString();
	useEffect(() => {
		if (!userId || !recurring || !existingNotifications) return;
		const snapshotKey = recurring.map((r) => `${r.id}:${r.next_due_date}`).join("|");
		if (ranFor.current === snapshotKey) return;
		ranFor.current = snapshotKey;
		(async () => {
			let touchedTransactions = false;
			const notificationsToInsert = [];
			for (const item of recurring) {
				if (!item.is_active) continue;
				const diff = daysUntil(item.next_due_date);
				if (diff <= 0) {
					const { error: insertError } = await supabase.from("transactions").insert({
						user_id: userId,
						account_id: item.account_id,
						category_id: item.category_id,
						amount: item.amount,
						type: item.type,
						merchant: item.merchant,
						payment_method: item.payment_method,
						description: item.description ?? `Recurring: ${item.merchant}`,
						transaction_date: item.next_due_date + "T09:00:00",
						status: "completed"
					});
					if (!insertError) {
						touchedTransactions = true;
						await supabase.from("recurring_transactions").update({ next_due_date: advanceDueDate(item.next_due_date, item.frequency) }).eq("id", item.id);
						const title = "Recurring transaction posted";
						const message = `${item.merchant} (₹${Number(item.amount).toLocaleString("en-IN")}) was added to your transactions.`;
						if (!existingNotifications.some((n) => n.title === title && n.message === message)) notificationsToInsert.push({
							user_id: userId,
							title,
							message,
							kind: "neutral"
						});
					}
				} else if (diff <= item.remind_days_before) {
					const title = "Upcoming bill";
					const message = `${item.merchant} (₹${Number(item.amount).toLocaleString("en-IN")}) is due on ${(/* @__PURE__ */ new Date(item.next_due_date + "T00:00:00")).toLocaleDateString("en-IN", {
						day: "numeric",
						month: "short"
					})}.`;
					if (!existingNotifications.some((n) => n.title === title && n.message === message)) notificationsToInsert.push({
						user_id: userId,
						title,
						message,
						kind: "warning"
					});
				}
			}
			if (notificationsToInsert.length > 0) {
				await supabase.from("notifications").insert(notificationsToInsert);
				queryClient.invalidateQueries({ queryKey: queryKeys.notifications(userId) });
			}
			if (touchedTransactions) {
				queryClient.invalidateQueries({ queryKey: queryKeys.recurringTransactions(userId) });
				queryClient.invalidateQueries({ queryKey: ["transactions", userId] });
			}
		})();
	}, [
		userId,
		recurring,
		existingNotifications,
		queryClient
	]);
	return {
		upcoming: (recurring ?? []).filter((r) => r.is_active && daysUntil(r.next_due_date) >= 0).filter((r) => daysUntil(r.next_due_date) <= Math.max(7, r.remind_days_before)).sort((a, b) => a.next_due_date.localeCompare(b.next_due_date)),
		today
	};
}
//#endregion
export { useUpdateRecurringTransaction as a, useNotificationSync as c, useRecurringTransactions as i, useNotifications as l, useDeleteRecurringTransaction as n, useMarkAllNotificationsRead as o, useRecurringTransactionSync as r, useMarkNotificationRead as s, useCreateRecurringTransaction as t };
